---
name: multi-review
description: claude（汎用・Fable 5）と codex（役割別）による複眼コードレビューを実行し、未解決の妥当な指摘がなくなるまで修正と再レビューを繰り返す。明示起動のみで発火する。トリガー例:「/multi-review」「複眼レビューして」「マルチレビューして」。通常の「レビューして」だけでは発火しない。
argument-hint: "[対象ディレクトリまたはファイルパス（省略時は git 差分）。「codex 抜きで」等でレビュアーを除外可]"
---

# multi-review

外部 CLI エージェント（claude＝汎用1インスタンス / codex＝役割ごと）に並列でコードレビューを依頼し、呼び出し元が指摘を選別・修正指揮し、未解決の妥当な指摘がなくなるまで再レビューを繰り返す。

用語: 呼び出し元＝このスキルを実行するメインセッション、レビュアー＝外部 CLI エージェント、指摘＝レビュアーが報告する個別の問題、妥当な指摘＝呼び出し元が実在かつ修正に値すると判定した指摘、選別＝指摘を妥当と却下に振り分ける行為、役割＝codex レビュアーに与える専門視点。（Step 3 以降に出てくる `CONTEXT.md` は対象プロジェクト側のファイルを指す）

実行の具体手順（コマンド形・対象埋め込み・スナップショット・品質チェック）は `references/execution.md` に置く。以下は呼び出し元への命令であり、呼び出し元は指摘の妥当性判定・修正の指揮・ループ終了の決定を担う。

## Step 1: 対象決定

- 引数はまず自然言語のレビュアー除外・限定指示（「codex 抜きで」「claude だけで」等）を抽出し、残りをパスとみなす。パスが残ればそれを対象にする。パスが残らなければ「引数なし」として以下の git 差分に従う。
- 引数がなく git リポジトリの場合: `git status --porcelain` で未コミット変更の有無を判定し、変更（staged / unstaged / untracked）があればそれを対象にする。変更がなければデフォルトブランチとの差分を対象にする。デフォルトブランチは `git symbolic-ref refs/remotes/origin/HEAD` → `main` → `master` の順に `git rev-parse --verify` で検出する。
- 未コミット変更がなく、デフォルトブランチとの差分も空なら中断し、ユーザーに対象を尋ねる。デフォルトブランチをいずれの方法でも検出できない場合も同様に中断して尋ねる。
- git リポジトリでなく引数のパスもない場合は中断し、ユーザーに対象を尋ねる。

## Step 2: レビューディレクトリ準備

- スクラッチパッド配下に `multi-review-<タイムスタンプ>/iter-1/` を作成する。スクラッチパッドはセッションのシステムプロンプトに記載された scratchpad ディレクトリ、なければ OS 一時領域を指す。対象リポジトリ内には作らない。
- 却下台帳 `ledger.md` をレビューディレクトリ直下（`multi-review-<タイムスタンプ>/ledger.md`）に作成する。却下台帳は本スキルの1回の実行内でのみ有効で、リポジトリには永続化しない。

## Step 3: レビュアー構成と役割選定

### レビュアー構成

- デフォルトは claude（汎用、Fable 5、1インスタンス）＋ codex（役割ごと）。cursor-agent は既定で無効とし、起動時の指示で明示された場合（「cursor-agent 込みで」「cursor-agent だけで」等）のみ構成に加える（コマンド形は `references/execution.md` に残置）。
- 起動時の指示で除外されたレビュアーは起動しない（例:「codex 抜きで」）。特定レビュアーだけに限定する指定も可（例:「claude だけで」）。除外・限定・cursor-agent 追加の指定は本スキルの1回の実行の全イテレーションに適用する。
- `command -v` で CLI の存在を確認し、未インストールのレビュアーは起動せず欠席として最終レポートに明記する。
- 除外・欠席の結果、起動できるレビュアーがゼロになる場合は中断し、ユーザーに報告する。
- codex を除外した場合、以下の役割選定はスキップする。

### 役割選定

- スキルのベースディレクトリ配下の `references/roles.md` の11役割（総合 / セキュリティ / バグハント / アーキテクチャ / パフォーマンス / テスト品質 / 保守性 / ドメイン整合性 / フロントエンド実装 / アクセシビリティ / UX・デザイン整合性）から選定する。総合は選定条件がなく原則毎回選ぶ。専門役割（総合以外の10役割）は変更内容を見て最大4個選ぶ（codex インスタンスは合計最大5）。総合が常駐するため codex は最低1インスタンス起動する（codex を除外した場合はレビュアー構成節に従い役割選定ごとスキップする）。
- ドメイン整合性は、対象プロジェクトに `CONTEXT.md` または `docs/adr/` が存在する場合のみ選定可。
- フロントエンド実装 / アクセシビリティ / UX・デザイン整合性は、対象にフロントエンドコード（JSX / TSX / Vue / Svelte / HTML / CSS / テンプレート）が含まれる場合のみ選定可。
- 役割ごとに codex の reasoning effort を選ぶ。既定は high（`~/.codex/config.toml` の既定と同じ）とし、高難度と判断した役割インスタンスのみ xhigh に上げる（判断基準の例: 並行処理・暗号・認可など深い推論を要する難所を含む、差分が大きく変更間の相互作用が広い）。
- 選んだ役割ごとに codex レビュアーを1インスタンス起動する（Step 4）。
- 役割選定後、実際に起動するレビュアー数を再確認し、ゼロなら中断してユーザーに報告する（構成節のゼロ件判定は選定前のため擦り抜ける）。

## Step 4: レビュアー並列起動

tmux は使わない。Bash ツールの `run_in_background: true` パラメータで各レビュアーをバックグラウンド起動し、出力をファイルに書かせる。全レビュアーを1メッセージ内で起動する。

各レビュアーの作業ディレクトリ・絶対パス規則・プロンプトの渡し方（codex / claude は stdin、cursor-agent はパス参照）・対象埋め込みの生成（未コミット / ベース差分の各モードと HEAD 不在フォールバック）・レビュアー別コマンド形・時間制限・出力の成功判定とリトライ/欠席は `references/execution.md` に従う。

### 共通報告フォーマット（全レビュアーに指示する）

このフォーマット定義を正とし、roles.md もこれを参照する。

```
### 指摘N（重要度: 高/中/低）
- ファイル: path:line（ファイル全体・欠落など行を特定できない指摘は path:- と書く）
- 問題: 1行で説明
- 修正案: 1行で提案
指摘がない場合は「問題なし」とだけ記載。重要度で取捨選択せず、低重要度も含め全て報告すること。
```

- 選別は呼び出し元の専権であり、レビュアーには重要度で取捨選択させず全報告させる。
- 全レビュアーが失敗したら中断し、ユーザーに報告する。
- 全バックグラウンドジョブの完了を待ち、各ジョブの終了コードと出力ファイルの中身を確認してから Step 5 へ進む。未完了の出力は読まない。

## Step 5: 選別（トリアージ）

- 全出力ファイルを読む。重複指摘（同一 file:line・同趣旨）を統合する。複数レビュアーが一致した「共通指摘」は、修正委譲プロンプトの先頭に並べ、最終レポートで別カウントする。
- 各指摘を「妥当（実在する かつ 修正に値する）」か「却下」に振り分ける。判定のために必要なら該当コードを読んで事実確認する。前回の不完全な修正で再報告された指摘は、新規の妥当な指摘として扱う。
- 各指摘の分類「妥当／却下」は排他とする。妥当な指摘には状態（未解決 / 修正済み / 保留）を持たせ、選別直後は未解決とする。
- 却下は理由必須で `ledger.md` に記録する（台帳には全件を残す）。
- 妥当だが影響範囲が大きい指摘（API 設計変更など）は、修正前に AskUserQuestion でユーザーに確認する。ユーザーが修正を見送った指摘は「保留」として `ledger.md` に記録し、再レビューでは再報告対象外とし、最終レポートには保留として残す。
- 未解決の妥当な指摘が1件以上あれば、Step 6 へ進む前に「修正方針サマリ」を会話に表示する: 妥当な指摘の一覧（各指摘に重要度・共通指摘の別・出所レビュアー・修正方針の1行サマリを付す）と、却下件数・保留件数。承認待ちはせず、表示後に Step 6 へ進む（ユーザーはいつでも中断して指示できる）。
- 未解決の妥当な指摘がゼロなら Step 8 へ進む。

## Step 6: 修正

- 初回イテレーションでは、後続の処理に先立って品質チェックの基準結果を記録する（手順は `references/execution.md`）。検出できなければ基準なしとして最終レポートに明記する。品質チェックが生成・更新するファイルを修正 diff の混入と誤判定しないよう、基準記録はスナップショットより前に行う。
- 続いてスナップショットを保存する（git / HEAD 不在 / 非 git の手順は `references/execution.md`）。
- スナップショット保存後、そのイテレーションの未解決の妥当な指摘を全件まとめて（保留・修正済みは除外）、1つの Agent ツール（general-purpose）に委譲する。共通指摘は指示の先頭に並べる。
- 委譲の指示に、指摘一覧（ファイル:行・問題・修正案）と、「検出した品質チェック（テスト・lint 等）を通すこと」「品質チェックの緩和・スキップ・`--no-verify` は禁止」「行を足す前に、既存文の修正・削除で対応できないか検討すること」を含める。
- 委譲した Agent が中断・エラー終了した場合は1度だけ再委譲し、それでも失敗したら差し戻し上限超過と同じ打ち切り報告（Step 8 の形式）をする。

## Step 7: 修正チェック → 再レビュー

- 修正 diff（委譲前スナップショットとの差分）を指摘と突き合わせ、全指摘への対応と、余計な変更の混入がないことを確認する。検出済みの品質チェックが生成・更新する種類のファイル（スナップショットテスト更新・ロックファイル・フォーマッタ整形等）は、修正エージェントが Step 6 で品質チェックを通した副産物なので「余計な変更の混入」から除外する（後述の品質チェック後の再照合と同種の扱い）。未対応の指摘や混入があれば、品質チェック失敗時と同じ経路で修正サブエージェントに差し戻す。
- 品質チェック（テスト・lint）を実行する（検出方法・時間制限・信頼前提・判定不能の定義は `references/execution.md`）。検出できなければ品質チェックなし（diff 照合のみ）で続行し、その旨を最終レポートに明記する。
- 失敗したら基準結果と照合する（既存の失敗・回帰・判定不能の分類規則は `references/execution.md`）。回帰なら修正サブエージェント（新しい Agent ツール）に失敗内容を渡して差し戻す。既存の失敗（非回帰）・基準なし・判定不能（時間制限超過を含む）は通過扱いで次へ進み、最終レポートに明記する。
- diff 照合起因と品質チェック起因の差し戻しは合算し、同一イテレーション内で2回まで（初回委譲後の失敗を1回目と数える）。超えたら打ち切りとして Step 8 の形式で報告する（未解決指摘と失敗内容を含める）。
- 品質チェックの実行で作業ツリーが変化した場合（スナップショットテスト更新・ロックファイル生成等）は、同じ委譲前スナップショットを基準に diff 照合を再実行する（品質チェックの実行ごとに1回。ループ化を避けるためその1回のみ。再照合は差し戻し回数に数えない）。
- diff 照合と品質チェックを通過したら、当該イテレーションで対応した指摘の状態を修正済みに更新する。
- 続いてイテレーション上限に達していないかを判定する。上限（5イテレーション）に達していれば Step 8 へ進み、上限到達による打ち切りとして報告する。達していなければ `iter-N+1` ディレクトリを作り Step 4 へ戻る。
- 再レビュー時は、各レビュアーのプロンプトに `ledger.md` の却下・保留一覧（「これらは検討済みのため再報告不要」）と前回指摘の修正概要を含める。ただし却下・保留の前提となった箇所がその後の修正で変わった台帳項目は再評価し、再報告不要一覧から外す。再レビューの対象範囲は、Step 1 で確定した範囲に当該イテレーションの修正差分を加えたもの（修正が対象外ファイルに及んだ場合はそのファイルも含める）とする。ベース差分モードの再レビューは codex の `--base <ブランチ>` を維持し、修正で生じた未コミットの差分は差分本文としてプロンプトに埋め込んで併せてレビューさせる（切替だとベース差分側の再検査が保証されないため）。
- 役割構成は原則維持し、修正が新領域に触れた場合のみ専門役割を追加する。専門役割は合計4個まで（総合を含め codex 合計最大5）とし、既に専門4役割を選定済みの状態で新領域に触れた場合は、総合を除く関連が最も薄い専門役割と入れ替えてよい。reasoning effort も原則維持するが、high の役割で見落とし・浅い指摘が疑われる場合は Step 3 の基準で xhigh に上げてよい。

## Step 8: 最終レポート（会話に日本語で出力）

以下を報告する。

- イテレーション数（上限到達で打ち切った場合はその旨を必ず記載）
- 修正した指摘数（重要度別・共通指摘数）
- 却下数と代表的な却下理由（全件と理由は `ledger.md`、代表例を本レポート、元の指摘はレビュー出力ログに残る）
- 保留した指摘（ユーザーが修正を見送ったもの）
- 欠席レビュアー
- 品質チェック結果。未実施の場合はラベルを書き分ける: 検出できず修正は実施した場合は「品質チェック未検出のため未実施（修正は実施）」、妥当な指摘ゼロで修正工程に進まなかった場合は「妥当な指摘ゼロで修正なしのため未実施」。時間制限超過で判定できなかった場合は「判定不能」と記載する。

詳細ログはレビューディレクトリに残る旨を1行添える。
