# multi-review

外部 CLI エージェント（codex / cursor-agent / claude）に並列でコードレビューを依頼し、指摘の妥当性を判定して修正し、未解決の妥当な指摘がなくなるまで再レビューを繰り返すスキル。

## Language

**呼び出し元（Caller）**:
このスキルを実行しているメインの Claude Code セッション。指摘の妥当性判定・修正の指揮・ループ終了の決定を担う。
_Avoid_: オーケストレーター、親エージェント

**レビュアー（Reviewer）**:
レビューを依頼される外部 CLI エージェント（codex / cursor-agent / claude）。指摘を出すだけで、妥当性の最終判定はしない。claude レビュアーは役割ごとに選定したモデル（既定 sonnet、高難度のみ opus）で起動する。
_Avoid_: 審査員、チェッカー

**指摘（Finding）**:
レビュアーが報告する個別の問題。ファイル位置・重要度・修正案を持つ。報告フォーマットのフィールド名「問題:」はこの Avoid の対象外。
_Avoid_: 問題、コメント、イシュー

**妥当な指摘（Valid finding）**:
呼び出し元が「実在する」かつ「修正に値する」と判定した指摘。却下と排他で、状態として未解決・修正済み・保留のいずれかを持つ。未解決の妥当な指摘がゼロになったらループ終了。

**選別（Triage）**:
呼び出し元が全指摘（低重要度含む）を「妥当」か「却下」に振り分ける行為。保留は妥当な指摘の状態であり、選別のラベルではない。レビュアーは重要度で取捨選択せず全報告し、選別の権限は呼び出し元だけが持つ。
_Avoid_: フィルタリング

**役割（Role）**:
claude レビュアーの1インスタンスに与える視点。役割カタログは総合 / セキュリティ / バグハント / アーキテクチャ / パフォーマンス / テスト品質 / 保守性 / ドメイン整合性 / フロントエンド実装 / アクセシビリティ / UX・デザイン整合性の11個で、呼び出し元は総合を原則毎回、専門役割（総合以外の10役割）を変更内容に応じて最大4個、動的に選定する（claude 合計最大5）。1役割=1インスタンス。codex / cursor-agent は役割を持たない汎用レビュアー。
_Avoid_: ペルソナ、観点（観点は役割内部のチェック項目を指す）

**総合（General reviewer）**:
役割の1つ。専門領域に限定せず全体を俯瞰し、専門役割の谷間に落ちる問題を検出する。選定条件はなく原則毎回選定する。

**ドメイン整合性（Domain consistency）**:
役割の1つ。対象プロジェクトの CONTEXT.md・ADR・用語集とコードの一致を検証する。該当ドキュメントがないプロジェクトでは選定しない。

**却下台帳（Dismissal ledger）**:
呼び出し元が却下・保留した指摘とその理由の記録。再レビュー時にレビュアーへ提示し、同じ誤検知の再出現によるループの空回りを防ぐ。本スキルの1回の実行内でのみ有効で、リポジトリには永続化しない。記録は3層に残る: 台帳に全件（理由含む）、最終レポートに代表例、レビュー出力ログには元の指摘。

**選定（Role selection）**:
呼び出し元が役割カタログから役割を選ぶ行為。総合を原則毎回、専門役割を変更内容に応じて最大4個選ぶ（claude 合計最大5）。役割ごとにモデル（既定 sonnet、高難度のみ opus）も併せて選ぶ。指摘を妥当と却下に振り分ける「選別」とは別の行為。

**共通指摘（Shared finding）**:
複数のレビュアーが一致して報告した指摘。修正委譲プロンプトの先頭に並べ、最終レポートで別カウントする。

**イテレーション（Iteration）**:
レビュー→選別→修正→修正チェックの1周期。上限は5。

**修正サブエージェント（Fix subagent）**:
妥当な指摘の修正を実装する Agent（general-purpose）。呼び出し元は委譲と指揮のみを担い、コードは書かない。

**欠席（Absent reviewer）**:
失敗・タイムアウト・未インストールで除外され、当該実行で起動できなかったレビュアー。最終レポートに明記する。

**保留（Deferred finding）**:
妥当な指摘の状態の1つ。ユーザーが修正を見送った指摘で、台帳に記録し、再レビューでは再報告対象外とし、最終レポートには保留として残す。

**品質チェック（Quality gate）**:
プロジェクトの構成ファイルや CLAUDE.md から検出するテスト・lint の実行。初回に基準結果を記録し、修正後の失敗が既存の失敗か回帰かの判定に使う。

**差し戻し（Rework request）**:
diff 照合・品質チェックの失敗内容を修正サブエージェントへ再委譲する行為。起因を合算して同一イテレーション内2回まで。
